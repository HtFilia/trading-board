[pytest]
markers =
    unit: Fast, isolated unit tests with no external I/O.
    integration: Cross-component tests exercising multiple layers (DB, Redis, services).
    e2e: Full end-to-end tests that require a running stack (executed in CI only).
